function formOnSubmit(form){
    const name = form.elements.name.value,
        sur = form.elements.sur.value,
        mail = form.elements.mail.value,
        oggetto = form.elements.oggetto.value,
        mex = form.elements.mex.value;

    //
}